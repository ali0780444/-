نظام إدارة الموارد البشرية المتكامل (HR Management System V29)

نظام سحابي متكامل لإدارة الموارد البشرية، يعتمد على Google Sheets كقاعدة بيانات و Google Apps Script كمعالج خلفي (Backend)، مع واجهة مستخدم حديثة ومتجاوبة.

🌟 المميزات

إدارة شاملة: غرامات، مكافآت، نسب، وتنبيهات إدارية.

تكامل مع تيليجرام: إرسال إشعارات فورية للموظفين عند تسجيل أي عملية.

واجهة متجاوبة: تعمل بكفاءة على الحاسوب، الأجهزة اللوحية، والهواتف.

إدارة الصلاحيات: نظام تسجيل دخول آمن مع صلاحيات محددة (HR, Admin, User).

نظام المتابعة والاعتراضات: دورة عمل كاملة لتقديم الاعتراضات ومراجعتها من قبل الـ HR.

حفظ جماعي: إمكانية اعتماد التغييرات في المتابعة دفعة واحدة.

تقارير ذكية: عرض التكرارات (Repetitions) للمخالفات، وسجل دوام دقيق.

🛠️ التقنيات المستخدمة

Frontend: HTML5, Tailwind CSS, JavaScript (Vanilla).

Backend: Google Apps Script (GAS).

Database: Google Sheets.

Integration: Telegram Bot API.

📂 هيكلية المشروع

index.html: ملف الواجهة الأمامية (يتم تشغيله في المتصفح).

Code.gs: كود المعالج الخلفي (يتم نسخه ووضعه داخل محرر Google Apps Script).

🚀 طريقة التثبيت والاستخدام

1. إعداد السيرفر (Google Sheets & Script)

قم بإنشاء Google Sheet جديد.

اذهب إلى Extensions > Apps Script.

انسخ محتوى ملف Code.gs من هذا المستودع وضعه في المحرر.

قم بتغيير الثوابت في بداية الكود (مثل TELEGRAM_TOKEN و TEST_GROUP_ID).

قم بالحفظ ثم انشر السكربت كـ Web App:

Execute as: Me (حسابك).

Who has access: Anyone (للتمكن من الوصول إليه من الواجهة).

انسخ رابط الـ Deployment URL.

2. إعداد الواجهة (Client Side)

حمل ملف index.html.

افتح الملف باستخدام أي محرر نصوص.

ابحث عن المتغير API_URL في كود الجافاسكربت:

const API_URL = "ضع_رابط_سكربت_جوجل_هنا";



استبدل الرابط بالرابط الذي نسخته في الخطوة السابقة.

احفظ الملف، ويمكنك الآن فتحه في المتصفح أو رفعه على استضافة (مثل GitHub Pages).

📸 لقطات الشاشة

(يمكنك إضافة صور للنظام هنا)

⚠️ ملاحظات هامة

هذا المستودع يحتوي على الكود المصدري فقط. البيئة التشغيلية تعتمد على حساب Google الخاص بك.

تأكد من عدم مشاركة رابط السكربت أو توكن التيليجرام علناً إذا كان المشروع خاصاً.

النظام يقوم بإنشاء أوراق العمل (Sheets) تلقائياً عند أول تشغيل إذا لم تكن موجودة.

تم التطوير بواسطة: 

$$اسمك أو اسم المطور$$